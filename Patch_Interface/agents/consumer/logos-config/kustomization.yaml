apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: consumer01
generatorOptions:
  disableNameSuffixHash: true

# ConfigMap para logos
configMapGenerator:
  - name: consumer-simpl-logo
    files:
      - simpl-logo.svg=logos/simpl-logo.svg
