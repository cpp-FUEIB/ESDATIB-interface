for pod in $(kubectl get pods -n consumer01 -o jsonpath='{.items[*].metadata.name}'); do
  echo "🔍 Pod: $pod"
  kubectl exec -n consumer01 "$pod" -- find / -name "*.json" 2>/dev/null
done | tee resultados_json.txt



kubectl exec -n consumer01 catalogue-ui-cfb6c5464-hx45h -- cat /app/node_modules/yargs/locales/en.json